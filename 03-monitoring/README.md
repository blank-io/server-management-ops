# Project 3: Python Resource Monitor

## Overview
A lightweight Python agent that logs CPU and Memory usage to a CSV file for historical analysis.

## Prerequisites
- Python 3.x
- `psutil` library (`pip install psutil`)

## Usage
Run the script in the background:
```bash
nohup python3 monitor.py &
